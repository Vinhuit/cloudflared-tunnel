{% raw %}
# Cloudflared Tunnel

Exposes any TCP service through Cloudflare Zero Trust tunnels using a simple Home Assistant integration.

## Configuration

Set hostname and port, then integration handles downloading and launching `cloudflared`.

Example:
- Hostname: `wyoming.mixsmart.dev`
- Port: `10300`
{% endraw %}